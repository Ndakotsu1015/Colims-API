models:
  ContractDocumentType:
    name: string
    softDeletes
    relationships:
      hasMany:
        AwardLetterRequiredDocument

controllers:
  ContractDocumentType:
    resource: api

seeders: ContractDocumentType

