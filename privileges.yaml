models:
  Privilege:
    name: string

  PrivilegeClass:
    name: string
  
  PrivilegeDetail:
    privilege_class_id: id foreign
    user_id: id foreign
    privilege_id: id foreign

  Menu:    
    title: string nullable
    link: string
    order: string nullable
    is_active: boolean default:false  
    icon: string nullable
    parent_id: id foreign:menu.id
    module_id: id foreign

  MenuAuthorization:
    menu_id: id foreign
    privilege_id: id foreign
  
controllers:
  Privilege:
    resource: api
  PrivilegeClass:
    resource: api
  PrivilegeDetail:
    resource: api
  Menu:
    resource: api
  MenuAuthorization:
    resource: api

seeder:  Privilege, PrivilegeClass, PrivilegeDetail, Menu, MenuAuthorization